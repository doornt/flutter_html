{
  "name": "flutter-render",
  "version": "0.1.0",
  "bin": "js/bin/index.js",
  "scripts": {
    "watch": "node js/bin/index.js --dir ./test/views --out ./test/outputs",
    "pub:run":"flutter packages pub publish --dry-run",
    "pub:publish":"flutter packages pub publish"
  },
  "dependencies": {
    "minimist": "^1.2.0",
    "pug": "^2.0.3"
  }
}
